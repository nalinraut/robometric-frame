source "https://rubygems.org"

# Matches the gem stack GitHub Pages uses for the automatic "pages build and
# deployment" workflow, so `bundle exec jekyll build` reproduces it locally/in CI.
gem "github-pages", group: :jekyll_plugins
